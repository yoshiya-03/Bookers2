<%= render 'layouts/error', local: @book %>


<div class="row">

  <%= render 'books/list', user: current_user, book: @book %>

  <div class="col-md-9">
    <h2>Books</h2>
      <div class="container">
        <table class="table">
          <thead>
            <tr>
              <th>Title</th>
              <th>Opinion</th>
              <th colspan="8"></th>
            </tr>
          </thead>
          <tbody>
          <tr>
            <% @books.each do |book| %>
            <td>
              <%= link_to user_path(book.user_id) do %>
      　       <%= attachment_image_tag book.user, :profile_image,  format: 'jpeg', fallback: "no-image.png",  size: "60x60" %>
            <% end %>
            </td>
              <td><%= link_to book.title, book_path(book.id) %></td>
              <td><%= book.body %></td>
            </tr>
            <% end %>
        </div>
      </div>
    </tbody>
  </table>
</div>



